import { MoveCategoryPipe } from './move-category.pipe';

describe('MoveCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
